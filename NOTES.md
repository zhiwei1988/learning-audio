# Teaching Notes

## Learner profile
- 即将接手网络摄像机音频（拾音 + 放音）
- 非音频专业；目标是产品音频竞争力设计，不是成为算法工程师
- 需要覆盖：软件、硬件、算法三条线
- 交流语言：中文；术语保留英文缩写（AEC、SNR、AOP…）

## Preferences
- **不要在一行里堆没解释过的新词。** 2026-08-03 反馈：表格里「切掉直流与低频隆隆：风噪、云台/结构振动、handling noise」一句同时塞了 4 个概念，其中 DC offset 和 handling noise 从未介绍过，读不懂。
  规则：新术语第一次出现必须给「中文 + 英文 + 一句它是什么」；表格单元格尤其要克制，宁可拆行。
- 英文术语不要裸奔在中文句子里（如 handling noise），要么给中文对照，要么进术语表并链接。
- **表格里的数字不能是「算好的结果」，要把算式摆出来。** 2026-08-04 反馈：第 7 课远场表有一列「配一颗 SNR 65 dB(A) 的麦，安静房间里还剩多少信噪比」，19 dB 这个数背后压了四步（datasheet SNR 在 94 dB SPL 下测 → EIN = 94−65 = 29 → 4 m 直达声 48 → 48−29 = 19），而我只把 `94 − SNR` 放在表下小字里，读不懂。
  规则：**凡是一格里做了多步换算，就把中间量拆成独立的列**（该表已改为「直达声 / 相对 1 m / 减去自噪声 29 dB / = 实际信噪比」），并在表前用 callout 讲清关键概念。
  同类风险点：任何 dB 相减、任何「其实是前面某课学过的量」。前课学过 ≠ 可以省略——要重述一遍再用（间隔重复的机会，不是负担）。
  另：给出一个数后要顺带给**读数标尺**（19 dB 到底算好还是差），否则用户拿到数字也没法判断。

## Component library (./assets)
- `styles.css` — 所有 lesson / reference 共用，Tufte 风格，含打印样式
- `quiz.js` — 单选即时反馈；选项字数必须对齐
- `mic-calc.js` — MEMS datasheet 数字 → 产品电平换算（第 2 课）
- `order-drill.js` — 通用「排出正确顺序」检索练习（第 4 课首用，可复用于信号链、评测流程排序）
- `band-limit.js` — 对数频率轴带宽可视化：选一个截止频率，看哪些语音特征被切掉（第 5 课首用，可复用于麦/喇叭频响、算法带宽）。**第 9 课扩展**：option 加 `"dir":"low"` 可改成从低频那一头切（喇叭 Fo 以下不发声），默认仍是高切
- `budget-builder.js` — 通用「预算分配」组件：逐项选档 → 实时总数 + 堆叠条 + 阈值线 + 最大项归因。支持 `own:false` 标记不可控项并单独统计（第 6 课首用，可复用于 MIPS 预算、码率预算、BOM 成本）
- `beam-pattern.js` — 极坐标波束图：选阵列类型（相加式/差分式）× 麦数 × 间距 × 频率 → 实时指向图 + DI / WNG / 波束宽度 / 混叠频率 + 自动结论。数学是真的（ULA 阵因子、一阶差分心形、球面积分算 DI），不是示意图（第 7 课首用，可复用于喇叭指向性、任何「孔径 vs 波长」的论证）

- `ab-repeat.js` — 「这个差异是真的吗」A/B 可复现性模拟器：真实差异 × 单次抖动 × 场次偏置 × 重复次数 × 测试设计（分开/配对）→ 散点 + 均值线 + 差值区间条 + 误差棒公式 + 2000 次重做的「对 / 测不出 / 反了」比例。
  数学是真的（解析误差棒与蒙特卡洛经验分布已对齐到小数点后三位），种子化 LCG 保证「重掷」是唯一变量。
  教的是「加测次数救不了错误的设计」——分开测的误差棒有地板 `1.96·√2·场次偏置`。可复用于任何 A/B 对比（麦 A vs B、算法版本、码率档位、MOS 面板）（第 8 课首用）
- `crossover-chart.js` — 「两种配置，谁赢取决于条件」曲线图：自动求交点、标翻转点、左右分区着色。每条曲线建模成「随扫描量变化的噪声 ⊕ 固定底噪」，适合任何「用固定代价换比例收益」的取舍（第 7 课首用，可复用于 NS 开关、麦 A vs 麦 B、装近 vs 装远）
- `playback-spl.js` — 放音链路 SPL 计算器 + 天花板归因：喇叭灵敏度（含任意参考距离）× 供电 × 阻抗 × 额定功率 × 听音距离 × 环境噪声 × THD → 十步换算表（每步一行，算式全摆出来）+ 天花板条形图（谁先到）+ **两本账并排结论**（响度余量 / ERLE 上限）。
  策略开关（保守峰值 vs 放开峰值）就是 Smart Amp 那 9.5 dB 的教学入口。数学是真的（BTL 功率、平方反比、12 dB 峰均比、−20·log₁₀(THD)）。
  可复用于任何「灵敏度 + 功率 + 距离 → SPL，且多个上限取最小」的论证：喊话号角选型、腔体改动、供电改动、喇叭 A vs B（第 9 课首用）
- `multi-ledger.js` — 「一组选择，多本账同时结算」。与 `budget-builder.js` 的分工：后者回答「这一项资源花了多少」，前者回答「同一个旋钮同时动四个数，谁先爆」。
  支持三种账本：**加法账**（各选项贡献相加）、**乘法**（`mul` 做全局缩放，如采样率减半）、**派生账**（`derived` 用步进表从某本账查出另一个量——专门用来表达「按档卖」的资源）。
  输出：每本账一条堆叠条 + 上限线 + 余量线 + 百分比，外加一句「先撞的墙是谁」。第 10 课首用（MIPS / 内存 / 功耗 / BOM），可复用于任何「多约束取最紧」的论证：码率×存储×带宽、板面积×成本×功耗、SPL 的四个天花板。
- `glossary-terms.js` + `glossary-link.js` — 术语双向链接。**terms 是生成物，别手改。**
  新增交互组件后要把它的根类名加进 `glossary-link.js` 的 `GL_SKIP_IN`，否则组件重渲染会把注进去的链接冲掉（`.mledger` 已加）。
- `build-glossary.mjs` — 生成器。**改完术语表、或新增一课之后必须跑一次：`node assets/build-glossary.mjs`**
- `build-mindmap.mjs` — 思维导图生成器，从 `glossary-terms.js` 生成 `reference/audio-mindmap.{mm,opml,md}`（FreeMind / OPML / 嵌套列表）。
  术语按**职能**分六支（声学基础 6 · 硬件 17 · 编码与网络 7 · 算法 23 · 评测 12 · 预算 11），严格树，一条术语只出现一次。
  文件里的 `TAXONOMY` 存的是术语表不知道的两件事：**归属哪一支**、**一句话本质**；15 条带数字的术语另有 `badge`，`｜` 之后必须写来源。
  **新增术语后跑 `node --test assets/build-mindmap.test.mjs` 会红**——测试断言分类表与术语表严格双射，逼你给新术语找位置，防止图和术语表悄悄漂开。

注：所有 lesson 用 `<script defer>` 而非 `type="module"`——模块脚本在 `file://` 下会被 CORS 拦掉，用户双击打开时交互会全部失效。
同理，术语数据走「生成一个 JS 数据文件」而不是运行时 `fetch('glossary.html')`——`file://` 下 fetch 一样被拦。

## 术语双向链接怎么工作
- **正向**（课文 → 定义）：`glossary-link.js` 运行时加链接。两条规则控制密度，避免整页变蓝：
  1. 所有 `<span class="term">`（作者已经手工标成术语的，全部变可点，外观不变）
  2. 每个 `<h2>` 小节内，每个术语的**第一次**裸文本提及，只链一次
  鼠标悬停显示定义摘要，多数时候不用跳转。交互组件（quiz/budget/beam/drill…）整体跳过——它们会自己重渲染，注进去的链接会被冲掉。
- **反向**（定义 → 课文）：`build-glossary.mjs` 扫全部页面，把「出现在：第 N 课 · …」写进 glossary.html 的每条 `<dd>` 里（静态，打印也在）。
- 别名从 `<dt>` 自动拆（中英混排会拆成中文段和英文段），两处需要人工兜底：
  - `ALIAS_OVERRIDE`：`<dt>` 拆出来的词太泛时手工指定（如 Reference → 参考信号，否则「参考」满页命中）
  - `aliasIsSafe`：2–3 字母的小写拉丁词一律不链（挡掉 Sibilance 贡献的 `sh`），大写缩写如 DI/NS 保留

## Lesson trajectory
1. 信号链全景 → 2. MEMS datasheet → 3. AEC / 全双工 → 4. NS + AGC + 处理链顺序 → 5. 编码 / 带宽 / 时延 → 6. 时延预算（九行账 + 缓冲↔AEC 尾长耦合）→ 7. 远场与波束成形（方向 ≠ 距离；孔径与间距两头夹；DI 换 WNG）→ 8. 评测方法（消声室买的是可复现性；配对四规则；误差棒；P.340 Behaviour 分级把「全双工」变成可查表的数）
→ 9. 放音侧（datasheet 三陷阱：10 cm 不是 1 m、频响下限写的是 Fo、额定功率是断续测的；四个天花板取最小；Smart Amp 卖的是「峰值上限从查表变成量出来」的 9.5 dB；**喇叭 THD 给 ERLE 设硬天花板**）
→ 10. 算力与预算（按峰值配不按平均配；模块之和 ≠ 系统总量，框架吃掉 47 % 内存；AEC 占 83 % 算力且 ∝ 尾长；**先撞的墙是内存不是算力**；算力按电压频率档卖，跨档才有钱拿；**尾长是第 6 课、第 3 课、第 10 课三本账的交点**）
下一步候选：**AV 同步与唇音对齐**（第 6 课只碰到音频单向时延，没碰音视频相对时延，而 MISSION 的 out-of-scope 明确把「音视频同步」留在范围内）。
再往后候选：**ERL / ERLE 的低成本实测手册**（RESOURCES Gaps 里唯一带方法论缺口的一条；第 3 课给了 dBFS 相减的思路但没给完整流程，第 8 课给了可复现性的框架，两边可以合起来）。已立为地图 task 工单 [#12](https://github.com/zhiwei1988/learning-audio/issues/12)，与本候选是**同一件事，别做两遍**。

**第 8 课补上的能力缺口值得记住**：前七课每一课都以「验收条款」收尾，但从没讲过那些数自己怎么测——条款没有测法就只是漂亮话。**今后每给出一条验收条款，都要顺带说明「这个数怎么自己测 / 能不能自己测」。**
学习者现在有了质疑数字的默认追问（同场吗？测几次？波动多大？成对报了吗？），后续评审类练习应假定他会问这些。

**第 9 课新增的默认追问**：拿到任何器件 spec，先问「这个数是在什么条件下量的」——
微型喇叭 SPL 常在 10 cm 量（比 1 m 高 20 dB），和第 2 课「麦 SNR 在 94 dB SPL 下测」是同一类陷阱。
**归一化再比较**应成为读 spec 的默认第一步。
另：第 9 课的 THD 表里有两格是占位值（1/10 额定 ≈2 %、1/2 额定 ≈5 %），已在表下明确标注为「按经验填的、必须实测替换」。
**今后凡是表里出现没有一手来源的数，都要就地标注，不能靠页脚一句免责带过。**

**第 10 课新增的默认追问**：拿到任何一个资源占用数字，先问「**这是平均还是峰值**」——
实时音频按 10 ms 帧交货，算不完就是丢帧，所以配硬件只认峰值，平均值只用来看趋势。
配套的第二问是「**先撞的是哪面墙**」：本课的例子里算力 79 %、内存 94 %，冲着算力优化会白干。
第三问是「**模块之和是不是系统总量**」——TIDUE77 的框架/驱动/缓冲占了内存的 47 %，不在任何模块账单上。

**第 10 课的外推做法值得沿用**：只有 128 ms 尾长是实测，其余档位按 ∝ 尾长线性外推，
并在页面上就地写明「这是保守上界（频域实现约 O(L log L)，真实更省）」。
**预算外推一律往坏的方向取整，并说清哪个数是实测、哪个是外推。**

五条已经贯穿全课的主线，后续课要继续挂上去：
- **木桶效应**（带宽断在最短一环，第 5 课）
- **缓冲统治时延**（第 6 课）
- **一个参数的改动会跨模块伤到别人**（AEC 参考取点 → 第 4 课；播放缓冲 → AEC 尾长 → 第 6 课；波束转向 → AEC 重收敛 → 第 7 课）
- **同一个动作在不同的账上价值完全不同**（第 7 课点破：换更好的麦在时延账上是零收益（6 µs），在差分阵列账上却决定成败。先问「在哪本账上算」）
- **同一个功能在不同条件下符号相反**（第 7 课五：波束成形在 38 dB(A) 以上净赚 4 dB，以下净赔 5 dB。凡是「要不要上 X」的问题，先问「在什么条件下」——然后画翻转曲线）
- **两本账，同一个旋钮符号相反**（第 9 课：音量往右拧，在响度账上是收益，在回声账上是损失——喇叭 THD 把 ERLE 锁在 −20·log₁₀(THD)）。它和「同一功能在不同条件下符号相反」的区别：那条是**换条件**翻转，这条是**换账本**翻转，同一时刻同时成立。凡是「要不要开大 X」的问题，先列出 X 影响的所有账本
- **收益是阶梯的，不是连续的**（第 10 课：算力按电压频率档卖，省 5 MIPS 一分钱不省，从 151 降到 149 MIPS 却省 82 mW。它和第 9 课 Smart Amp 那 9.5 dB 是同一种形状——收益不在连续的努力里，在跨过某条线的那一刻。凡是「优化到多少合适」的问题，先去找那条线在哪）
- **绝对分不可复现，差值可复现**（第 8 课：P.340 自己承认消声室是 "for the repeatability of the tests"；MOS 的 ICC 0.719 vs DMOS 0.907。拿到任何一个数，先问「它是绝对值还是差值、跟什么比的、同场测的吗」）

## Teaching strategy
- 每课一个可迁移技能，绑定 MISSION 中的「竞争力决策」
- 先建立信号链心智模型，再分硬件 / 算法 / 编码网络 / 评测
- 资源优先一手：ADI、Infineon、TI、ONVIF、ITU
- Lessons 用中文讲解 + 英文术语；引用 RESOURCES 中的链接
